import React, { useEffect, useState } from "react"; import { Eye } from "lucide-react"; export default function ContatoreVisite() { const [totale, setTotale] = useState<number | null>(null); useEffect(() => { let cancelled = false; fetch("/api/contatore-visite").then((res) => (res.ok ? res.json() : null)).then((data) => { if (!cancelled && data && typeof data.totale === "number") { setTotale(data.totale); } }).catch(() => {}); return () => { cancelled = true; }; }, []); if (totale === null) return null; return React.createElement("div", { className: "hidden sm:flex items-center gap-1.5 px-2.5 py-1 border-2 border-foreground bg-muted text-xs font-medium", title: "Visite totali al sito", "data-testid": "contatore-visite" }, React.createElement(Eye, { className: "w-3.5 h-3.5", "aria-hidden": "true" }), React.createElement("span", null, totale.toLocaleString("it-IT") + " visite")); }
